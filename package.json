{
  "name": "cbs-league-history",
  "version": "0.1.0",
  "description": "Scrape and archive a CBS Sports fantasy league's full history, then serve it as a dashboard.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node src/cli.js check",
    "crawl": "node src/cli.js crawl",
    "report": "node src/cli.js report",
    "test": "node --test test/run-tests.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
